# StockChronicle 作業メモ

最終更新: 2026-09-06

公開サイト https://stockchronicle.app は、エラーなく動作している。GitHub リポジトリは非公開のまま、Cloudflare の無料枠で配信している。

## 目的

- ネット上のページで、直近1年の営業日ヒートマップを日付切替できるようにする
- 同じ1年分の、経済・株式の主要ニュース／出来事も日付ごとに見られるようにする
- 全期間の生データ（history/）は GitHub に上げない
- リポジトリは非公開のままにする

## 見る場所

| 場所 | URL / 操作 | 見える範囲 |
|------|------------|------------|
| 公開サイト | https://stockchronicle.app | 直近1年の終値ヒートマップと市場ニュース |
| この PC | start.bat → http://127.0.0.1:8765 | history/ があれば全期間。無ければ GitHub 上の1年分 |

ローカルフォルダ: `D:\For_work01\stockchronicle`  
GitHub（非公開）: https://github.com/mnghfxiidm4ad-rgb/Stock-Price-Heat-Map

## 公開しているデータ（直近1年）

最初に用意した範囲（2026-09-06 時点）:

| 内容 | 日本株 | 米株 |
|------|--------|------|
| 終値ヒートマップ | 247営業日（2025-09-01〜2026-09-04） | 255営業日（2025-09-02〜2026-09-04） |
| 市場ニュース | 同じ期間 | 同じ期間 |

保存先:

- 日次終値: `data/quotes/jp/YYYY-MM-DD.json` と `data/quotes/us/YYYY-MM-DD.json`（コンパクト配列）
- 日付一覧: `data/quotes/jp/index.json` と `data/quotes/us/index.json`
- 市場ニュース: `data/market_news/jp/YYYY-MM-DD.json` と `data/market_news/us/YYYY-MM-DD.json`
- 最新日の全体スナップショット: `data/jp.json` / `data/us.json`

1年より古い日次ファイルは、日次更新のときに削除する（目安 370 日）。  
`history/`（全期間の parquet）は `.gitignore` 対象で、GitHub にも Cloudflare にも上がらない。

1年分を作り直すとき:

```bat
.\.venv\Scripts\python.exe scripts\build_web_year.py --quotes-only
.\.venv\Scripts\python.exe scripts\build_web_year.py --news-only --no-llm --sleep 0.25
```

ニュースは LLM キーが無い環境では、指数の騰落＋ Google News の見出しで書く。

## なぜ GitHub Pages ではないか

最初は GitHub Pages で出そうとしたが、非公開リポジトリでは今のプランで Pages を作れなかった。

GitHub の返答: Your current plan does not support GitHub Pages for this repository.

独自ドメインを付けても、この制限は変わらない。非公開のまま Pages を使うには GitHub Pro が必要。  
そのため GitHub Pages 用ワークフロー（`.github/workflows/pages.yml`）は削除した。

公開ページ自体は、Cloudflare でも GitHub Pages でも誰でも見られる。非公開なのはリポジトリの中身（ソースと全履歴）だけ。

## Cloudflare の公開方法

ドメイン `stockchronicle.app` は Cloudflare で取得済み。Workers / Pages の Git 連携で、非公開 GitHub リポジトリを読んで静的ファイルだけ出す。

Cloudflare プロジェクト名: `stock-price-heat-map`（GitHub リポジトリ名から付いた）

ダッシュボードで動いている設定:

| 項目 | 値 |
|------|-----|
| Build command | `python3 scripts/prepare_pages_site.py` |
| Deploy command | `npx wrangler deploy` |
| Root directory | `/` |
| 出力先 | wrangler.toml の assets.directory = _site |

画面に「Build output directory」が無いことがある。今の作成画面は Worker 寄りで、出力先は `wrangler.toml` を見る。Create の初期画面が Workers になっていることも多い。Pages タブを選ばなくても、上記の Worker 設定で公開できている。

`scripts/prepare_pages_site.py` は、公開に必要なものだけを `_site` にコピーする。

- 入れる: `index.html` / `static/` / `data/quotes/` / `data/market_news/` / `data/jp.json` / `data/us.json` / `_headers`
- 入れない: `history/` / `.venv/` / Python ソース / GitHub Actions

リポジトリ直下をそのまま出すと、スクリプトまで URL で見えてしまう。

`main` へ push するたびに Cloudflare が再ビルドする。終値・ニュースの日次 Actions が GitHub に書き戻すと、公開サイトも更新される。

## wrangler.toml

```toml
name = "stock-price-heat-map"
compatibility_date = "2026-09-06"

[assets]
directory = "_site"
```

`name` は Cloudflare のプロジェクト名と一致させる。最初は `stockchronicle` にしてあり、ダッシュボードが名前の不一致を出していた。

## 日次の自動更新（GitHub Actions）

リポジトリは非公開のままでも Actions は動く。

| ワークフロー | 内容 |
|--------------|------|
| Update closing prices | 日本株 17:00 / 19:00 JST、米株 16:00 ET 以降に終値を取得。`data/jp.json` `data/us.json` とコンパクト日次を更新し、1年より古い日次は削除 |
| Update JP market news | 16:00 JST 以降に日本株ニュース |
| Update US market news | 米引け後（目安 06:30 JST、夏時間は 05:30 JST） |

Secrets に `GEMINI_API_KEY` か `OPENAI_API_KEY` があると、日次ニュースの文面が良くなる。無くても見出しベースで動く。

## 公開までのトラブルと対処

1. GitHub Pages  
   非公開＋現行プランではサイトを作成できない（422）。Cloudflare に切り替えた。

2. 「Build output directory」が無い  
   今の画面では出ないことがある。出力先は `wrangler.toml` の `_site`。Build command だけ入れればよい。

3. wrangler.toml の名前不一致  
   プロジェクト名は `stock-price-heat-map`。`name` を合わせた。

4. Permission denied  
   `scripts/prepare_pages_site.sh` に実行権限が無かった。`git update-index --chmod=+x` した。

5. cannot execute binary file  
   Windows で `.sh` が UTF-16 で保存され、Linux の bash がバイナリ扱いした。公開準備は UTF-8 の `scripts/prepare_pages_site.py` に切り替えた。`.gitattributes` で `.sh` `.py` `.toml` を LF に固定。

6. ビルド中の pip install -r requirements.txt  
   Cloudflare がルートの requirements.txt を見て pandas などを入れる。公開サイトでは使わない。時間がかかるだけで害は無い。

## この PC でよく使うコマンド

```bat
start.bat
fetch_history.bat
update_quotes.bat
collect_news.bat
.\.venv\Scripts\python.exe scripts\build_web_year.py
.\.venv\Scripts\python.exe scripts\prepare_pages_site.py
```

## 主なファイル

| ファイル | 役割 |
|----------|------|
| `index.html` / `static/app.js` | 画面。静的サイトでは `data/quotes/` と `data/market_news/` を日付で読む |
| `scripts/build_web_year.py` | ローカル history から直近1年の終値・ニュースを書き出す |
| `scripts/prepare_pages_site.py` | Cloudflare 用に `_site` を作る（公開ファイルだけ） |
| `scripts/update_quotes.py` | 日次終値。コンパクト日次の追加と1年超の削除 |
| `news/` | 市場ニュース収集 |
| `wrangler.toml` | Cloudflare プロジェクト名と出力ディレクトリ |
| `deploy/_headers` | 公開サイトのキャッシュとセキュリティヘッダ |
| `.gitignore` | `history/` `.venv/` `_site/` を除外 |

## あとからやること（任意）

- Cloudflare の Custom domains に `stockchronicle.app` を付けていなければ付ける（ドメインは取得済み）
- 日次ニュースを厚くするなら、GitHub の Secrets に `GEMINI_API_KEY` を入れる
- 公開サイトで日付切替と日米切替、ニュース表示を時々確認する
